package listing_03_12

// ------------------------------------------------------------------
// Listing 3.12 - Using `if` as a statement rather than an expression
// ------------------------------------------------------------------

fun main() {
	var wattage = 0
	var lightbulbIsOn = true
	
	if (lightbulbIsOn) {
	    wattage = wattage + 12
	}
}
